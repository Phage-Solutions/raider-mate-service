headers {
  Authorization: Bearer {{serviceApiKey}}
}

docs {
  These two routes are the bot's outbox, called by the bot process on its own behalf
  rather than on a raider's. They sit behind the shared key alone, with no actor
  headers, so one claim spans every guild.
}
